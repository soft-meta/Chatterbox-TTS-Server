# Isolated Qwen3-TTS VoiceDesign environment.
# Installed in a separate micromamba environment to avoid Transformers conflicts
# with the Chatterbox runtime.
qwen-tts==0.1.1
speechbrain==1.0.3
soundfile==0.13.1
scipy==1.15.3
setuptools==80.9.0
