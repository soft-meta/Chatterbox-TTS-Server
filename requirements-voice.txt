# Install this file inside a separate virtual environment created with
# --system-site-packages. Parler-TTS 0.2.3 requires Transformers 4.46.1,
# while Chatterbox 0.1.7 uses a newer incompatible Transformers release.
parler-tts==0.2.3
soundfile==0.13.1
