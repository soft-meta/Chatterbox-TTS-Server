# Isolated MOSS VoiceGenerator environment.
# Install the official OpenMOSS/MOSS-TTS repository first so its custom
# Transformers model and audio-tokenizer dependencies are available.
transformers==5.0.0
speechbrain==1.0.3
soundfile==0.13.1
scipy==1.15.3
huggingface-hub>=0.36.0
setuptools==80.9.0
